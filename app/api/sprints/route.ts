import { NextResponse } from "next/server";

// Placeholder — will be replaced with Cabana creation endpoint
export async function POST() {
  return NextResponse.json({ error: "not implemented" }, { status: 501 });
}
